/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Si más adelante quieres i18n, agrégalo aquí:
  // i18n: {
  //   locales: ['es', 'en'],
  //   defaultLocale: 'es',
  // },
};

module.exports = nextConfig;
